# Tests

`fixtures/stage-outputs/` contains observed historical outputs. Fixtures are evidence for evaluation and regression design; they are not automatically expected outputs or canonical knowledge.

Future tests should distinguish structural conformance, grounding quality, exclusion violations, and semantic review.
